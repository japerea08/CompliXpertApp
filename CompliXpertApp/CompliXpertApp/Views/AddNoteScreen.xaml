<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CompliXpertApp.Views.AddNoteScreen"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.Content>
        <StackLayout Margin="15,0">
            <Label
                Margin="0,5"
                FontAttributes="Bold"
                FontSize="Large"
                HorizontalTextAlignment="Center"
                Text="Add Note" />
            <Label
                Margin="0,5"
                FontSize="Medium"
                Text="Subject" />
            <Entry FontSize="Medium" Text="{Binding Subject}" />
            <Label
                Margin="0,5"
                FontSize="Medium"
                Text="Description" />
            <Entry FontSize="Medium" Text="{Binding Description}" />
            <Button
                Margin="0,5"
                BackgroundColor="#428bca"
                Command="{Binding SaveNoteCommand}"
                Text="Save"
                TextColor="White" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>